<h2><%= "#{@note.title} - #{@note.money}" %><h2/>

<%= link_to "Edit Note", edit_month_note_path(@month, @note) %>

<%= link_to "Delete Note", month_note_path(@month, @note), method: :delete, data: { confirm: "Are you sure you want to delete this note?" } %>
